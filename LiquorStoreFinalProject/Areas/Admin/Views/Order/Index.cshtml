@using LiquorStoreFinalProject.ViewModels;
@model GetPaginatedOrdersVM;
@{
    ViewData["Title"] = "Index";
}

<div class="container my-3">
    <h2 class="text-center">Products</h2>
    <div class="table-responsive">
        @if (Model.Orders.Any())
        {

            <table class="table table-striped">
                <thead>
                    <tr>
                        <th>
                            Content
                        </th>
                        <th>
                            Code
                        </th>
                        <th>
                            Total Price
                        </th>

                    </tr>
                </thead>
                <tbody>
                    @foreach (var order in Model.Orders)
                    {
                        <tr>

                            <td class="py-1">
                                @order.Content
                            </td>
                            <td class="py-1">
                                @order.Code
                            </td>
                            <td class="py-1">
                                @order.TotalPrice
                            </td>

                            @*<td>
                    <a href="@Url.Action("UpdateProduct", "Product", new { id = product.Id })" class="btn btn-warning btn-rounded btn-fw">Edit</a>
                    <a href="@Url.Action("DeleteProduct", "Product", new { id = product.Id })" class="btn btn-danger btn-rounded btn-fw">Delete</a>
                    </td>*@
                        </tr>

                    }

                </tbody>
            </table>
            <div class="pagination d-flex justify-content-center mt-3">
                <nav aria-label="...">
                    <ul class="pagination">

                        @for (int i = 1; i <= Model.Pages; i++)
                        {
                            <li class="page-item @(Model.CurrentPage == i ? "disabled" : "")">
                                <a class="page-link " href="@Url.Action("Index", new { page = i })">@i</a>
                            </li>
                        }
                    </ul>
                </nav>
            </div>
        }
        else
        {
            <h1>No  Orders</h1>
        }
    </div>
</div>


